version: '3'

services:
    postgres:
        image: 'postgres:15.2'
        environment:
            POSTGRES_PASSWORD: postgres
            POSTGRES_USER: postgres
            POSTGRES_DB: postgres
        ports:
            - 5434:5432
