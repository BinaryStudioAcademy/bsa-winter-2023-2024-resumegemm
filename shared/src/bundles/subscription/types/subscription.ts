type Subscription = {
    id: string;
    status: string;
    subscriptionId: string;
    subscriptionPlanId: string;
    userId: string;
    createdAt: string;
    updatedAt: string;
};

export { type Subscription };
