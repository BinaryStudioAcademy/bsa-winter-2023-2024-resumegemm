type Contacts = {
    id: string;
    resumeId: string;
    phoneNumber: string;
    socialContact: string;
    link: string;
    createdAt: string;
    updatedAt: string;
};

export { type Contacts };
