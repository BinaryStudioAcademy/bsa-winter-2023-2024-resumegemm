type Contacts = {
    id: string;
    resumeId: string;
    mobileNumber: string;
    homeNumber: string;
    address: string;
    socialContact: string;
    createdAt: string;
    updatedAt: string;
};

export { type Contacts };
