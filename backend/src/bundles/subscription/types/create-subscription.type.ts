type CreateSubscription = {
    status: string;
    subscriptionId: string;
    userId: string;
    subscriptionPlanId: string;
    startDate: Date;
    endDate: Date;
};

export { type CreateSubscription };
