enum AuthMessage {
    RESET_TOKEN_SENT = 'Password reset token sent to email',
    PASSWORD_RESET = 'Password has been reset',
    RESET_TOKEN_CORRECT = 'Reset token is correct',
}

export { AuthMessage };
