type Subscription = {
    id: string;
    customerId: string;
    subscriptionId: string;
    isActive: boolean;
    subscriptionStart: string;
    subscriptionEnd: string;
};

export { type Subscription };
